pandas>=2.2
numpy>=1.26
scipy>=1.13              # norm.pdf for BS gamma in factors/gex.py
scikit-learn>=1.4          # ML-P8B.3 simple learned baselines (Ridge / LogisticRegression)
pillow>=10.0                 # rounded terminal favicon generation (scripts/generate_terminal_favicon.py)
pyarrow>=15.0
yfinance>=0.2.40
requests>=2.32
pyyaml>=6.0
python-dateutil>=2.9
python-dotenv>=1.0          # load secrets from project-root .env
fastapi>=0.115              # Ultimate Terminal M4 local web API
uvicorn[standard]>=0.32      # serve Terminal UI
httpx>=0.27                 # FastAPI TestClient in tests
ijson>=3.2                  # stream-parse large GEXBot hist JSON without OOM

# ThetaData optional — cloud vendor deploy does not need it. Local rollback:
#   pip install -r requirements-thetadata.txt

pytest>=8.0
pytest-mock>=3.12

ruff>=0.5
